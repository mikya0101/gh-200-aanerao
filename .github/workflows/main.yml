name: Workflow01

on: push

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello, Amol Anerao
        run: echo "Hello, Amol Anerao"

      - name: Copy Repo
        uses: actions/checkout@v6

      - name: List Files
        run: ls -la 
          
  
